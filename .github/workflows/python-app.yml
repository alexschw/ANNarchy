# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-python-with-github-actions

name: Linux Tests

on: [push]

permissions:
  contents: read

jobs:
  flakeLint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.6
      uses: actions/setup-python@v3
      with:
        python-version: "3.6"
    - name: Install flake
      run: pip install flake8
    - name: Lint with flake8
      run: |
        # stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --ignore=F723 --exclude=benchmarks/ --show-source --statistics
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
  single_thread:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.6
      uses: actions/setup-python@v3
      with:
        python-version: "3.6"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest coverage
        pip install -r requirements.txt
        python setup.py install
    - name: Test single threaded
      run: |
        cd tests/
        coverage run -a test_single_thread.py
        coverage xml -o coverage_st.xml
        cd ..
    - uses: actions/upload-artifact@v3
      with:
        name: cov_st
        path: tests/coverage_st.xml
  openMP:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.6
      uses: actions/setup-python@v3
      with:
        python-version: "3.6"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest coverage
        pip install -r requirements.txt
        python setup.py install
    - name: Test OpenMP Code
      run: |
        cd tests/
        coverage run -a test_openmp.py
        coverage xml -o coverage_omp.xml
        cd ..
    - uses: actions/upload-artifact@v3
      with:
        name: cov_omp
        path: tests/coverage_omp.xml
  CUDA:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.6
      uses: actions/setup-python@v3
      with:
        python-version: "3.6"
    - name: cuda-toolkit
      uses: Jimver/cuda-toolkit@v0.2.7
    - run: nvcc -V
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest coverage
        pip install -r requirements.txt
        python setup.py install
    - name: Test CUDA Code
      run: |
        cd tests/
        coverage run -a test_CUDA.py
        coverage xml -o coverage_cu.xml
        cd ..
    - uses: actions/upload-artifact@v3
      with:
        name: cov_cu
        path: tests/coverage_cu.xml
  Codecov:
    runs-on: ubuntu-latest
    needs: [single_thread, openMP, CUDA]
    if: always()
    steps:
      - uses: actions/checkout@v3
      - uses: actions/download-artifact@v3
      - name: Upload to Codecov
        uses: codecov/codecov-action@v3
        with:
          flags: unittests
          
